services:
  web:
    build:
      context: .
      args:
        VERSION: ${APP_VERSION:-2.0.0}
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      APP_VERSION: ${APP_VERSION:-2.0.0}
